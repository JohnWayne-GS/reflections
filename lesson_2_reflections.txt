What happens when you initialize a repository? 
	The git init command creates a new Git repository. It can be used to convert an existing, unversioned project to a Git repository or initialize a new, empty repository.

Why do you need to do it?
	Most other Git commands are not available outside of an initialized repository, so this is usually the first command you'll run in a new project.

How is the staging area different from the working directory and the repository?


	it gives you a message which files are staged, unstaged, and untracked

What value do you think it offers?
	Avoid mistakes, always good overview



How can you use the staging area to make sure you have one commit per logical
change?
	put more files and changes into staging area and then commit when a logical step is reached




What are some situations when branches would be helpful in keeping your history
organized? 
	Branches helps you to work separetly from the master without touch a running structure
	Master branch is your working/production branch

How would branches help?
		



How do the diagrams help you visualize the branch structure?


	otherwise you would forget all teh branch constellation

What is the result of merging two branches together? 
	merge all commits together and choose what you wanna keep and what not

Why do we represent it in 
the diagram the way we do?
	to understand visually how branches and maps stay together



What are the pros and cons of Git's automatic merging vs. always doing merges 
manually?
	
	pros: avoiding mistakes